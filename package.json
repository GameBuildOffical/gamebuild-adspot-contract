{
  "name": "gamebuild-adspot-contract",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2",
    "prettier": "^3.1.0",
    "@types/node": "^20.11.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
